

// const API_BASE_URL = 'http://localhost:3001/api/files';

export const fetchPrograms = async () => {
    return undefined;
};

export const fetchFiles = async (programKey: string) => {
    return undefined;
};

export const saveFile = async (programKey: string, fileName: string, content: string) => {
    return undefined;
};

export const deleteFile = async (programKey: string, fileName: string) => {
    return undefined;
};

export const saveProgram = async (programKey: string, programName: string) => {
    return undefined;
};
